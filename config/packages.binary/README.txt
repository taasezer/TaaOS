TaaOS Build System - Binary Packages
======================================

Place Debian package files (.deb) here to include them on the ISO
but NOT install them by default.

Use this for:
1. Providing optional software (e.g., NVIDIA drivers, offline tools).
2. Packages that the user might want to install later manually (dpkg -i).
3. Creating a repository structure on the disc itself.

---
mount:
    - device: /dev/sda1
      fs: vfat
      mountPoint: /boot/efi
      options: [ defaults, umask=0077 ]
    - device: /dev/sda2
      fs: ext4
      mountPoint: /
      options: [ defaults, noatime ]

TaaOS Build System - Local Packages
=====================================

Place valid Debian package files (.deb) here.
They will be automatically installed into the system during the build.

Rules:
1. Files must have the .deb extension.
2. Dependencies must be resolvable from the configured repositories.
3. This is useful for proprietary software or custom builds not in repos.
